{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://glbforge.dev/schemas/0.5.0/generation_status.input.json",
  "title": "generation_status.input",
  "description": "Arguments of the generation_status tool.",
  "type": "object",
  "properties": {
    "requestId": {
      "type": "string"
    },
    "model": {
      "type": "string",
      "enum": [
        "hunyuan",
        "trellis",
        "triposr"
      ]
    },
    "download": {
      "type": "boolean",
      "default": false
    },
    "out": {
      "type": "string",
      "description": "Absolute output path for the .glb when downloading"
    },
    "preview": {
      "type": "string",
      "enum": [
        "thumbnail",
        "turntable",
        "compare",
        "none"
      ],
      "default": "thumbnail",
      "description": "Rendered PNG of the downloaded model returned with the result: thumbnail (one 3/4 view) | turntable (4 angles tiled 2x2) | none"
    },
    "render": {
      "type": "boolean",
      "description": "Checkpoint render: true = attach a thumbnail even if preview=none, false = no image (overrides preview)"
    },
    "dry_run": {
      "type": "boolean",
      "default": false,
      "description": "Compute everything in memory and return the diff it would apply; write nothing"
    }
  },
  "required": [
    "requestId",
    "model"
  ],
  "additionalProperties": false
}
