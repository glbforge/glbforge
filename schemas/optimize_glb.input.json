{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://glbforge.dev/schemas/0.8.0/optimize_glb.input.json",
  "title": "optimize_glb.input",
  "description": "Arguments of the optimize_glb tool.",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "Absolute path to the input .glb"
    },
    "out": {
      "type": "string",
      "description": "Output path (default: <input>.web.glb)"
    },
    "profile": {
      "type": "string",
      "description": "Budget profile: mobile-hero | desktop-hero | product-configurator; pin a version with name@N (e.g. mobile-hero@1)",
      "default": "mobile-hero"
    },
    "targetTriangles": {
      "type": "integer",
      "exclusiveMinimum": 0
    },
    "lods": {
      "type": "array",
      "items": {
        "type": "integer",
        "exclusiveMinimum": 0
      },
      "description": "Extra LOD triangle targets, e.g. [40000, 10000]"
    },
    "textures": {
      "type": "boolean",
      "default": true
    },
    "compress": {
      "type": "boolean",
      "default": true
    },
    "textureFormat": {
      "type": "string",
      "enum": [
        "webp",
        "ktx2"
      ],
      "default": "webp",
      "description": "webp = smallest file; ktx2 = GPU-resident compression, ~8x less video memory (needs basisu/toktx installed)"
    },
    "verify": {
      "type": "boolean",
      "default": true,
      "description": "Perceptual verification: SSIM of fixed-camera renders before vs after, gated on the profile floor; a failing SSIM fails the report"
    },
    "preview": {
      "type": "string",
      "enum": [
        "thumbnail",
        "turntable",
        "compare",
        "none"
      ],
      "default": "thumbnail",
      "description": "thumbnail | turntable | compare (reference | result | change heatmap of the weakest view) | none. A failing SSIM always returns the comparison sheet."
    },
    "render": {
      "type": "boolean",
      "description": "Checkpoint render: true = attach a thumbnail even if preview=none, false = no image (overrides preview)"
    },
    "dry_run": {
      "type": "boolean",
      "default": false,
      "description": "Compute everything in memory and return the diff it would apply; write nothing"
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
