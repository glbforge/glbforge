{
  "name": "glbforge-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "build:icons": "node scripts/build-icons.mjs",
    "build:og": "node scripts/build-og.mjs",
    "probe": "pnpm --filter @glbforge/mcp probe",
    "ledger": "node scripts/ledger.mjs",
    "live-check": "node scripts/live-check.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  },
  "packageManager": "pnpm@12.4.1"
}
