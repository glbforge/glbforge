node_modules/
dist/
.env
.env.*
*.log
examples/viewer/node_modules/

# generated example outputs (reproducible via the CLI; keep the repo light)
examples/*.glb
examples/viewer/public/*.glb
# .claude/ holds worktrees and local launch config, but the agent-loop skill
# has to be committed: a scheduled cloud pass checks the repo out and finds
# its instructions there, not on this machine.
.claude/*
!.claude/skills/
training/

# live-check history + launchd log (machine-local, regenerated hourly)
.agent-loop/

# macOS metadata — never ship these as site assets
.DS_Store
